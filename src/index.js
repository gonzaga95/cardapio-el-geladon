import React from "react";
import ReactDOM from "react-dom";

ReactDOM.render(
  <React.StrictMode>Primeiro projeto. Iniciando os commits. Módulo 04 do curso de FullStack da Blue EdTech</React.StrictMode>,
  document.getElementById("root")
);